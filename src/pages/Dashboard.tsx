import StatsCard from '../components/StatsCard';
const Dashboard = () => {
  return (
    <div>
      <StatsCard />
      {/* Add more sections such as charts, recent activities, etc. */}
    </div>
  );
};

export default Dashboard;
